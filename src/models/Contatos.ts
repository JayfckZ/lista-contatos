class Contatos {
  nome: string
  numero: string
  id: number
  email: string

  constructor(nome: string, numero: string, email: string, id: number) {
    this.nome = nome
    this.numero = numero
    this.id = id
    this.email = email
  }
}

export default Contatos
